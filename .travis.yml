language: cpp
os:
- linux
compiler:
- gcc
sudo: required
services: docker
before_install:
- docker build -t miladh/lgn-simulator -f .docker/Dockerfile .
script:
- docker run miladh/lgn-simulator bash -c "/opt/qt57/bin/qmake lgnSimulator.pro && make
  -j8 "
